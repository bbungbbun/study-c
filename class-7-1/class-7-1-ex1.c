#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int get_max(int x, int y){
    if(x > y) return (x);
    else return (y);
}

int main(void) {
	int x, y, max;

    printf("두 정수를 입력하시오");

	return 0;
}